# distinct-plugins

Plugins for **Distinct**: query BigQuery with strong context and control inside your AI-assisted workflow.

This repository hosts the Claude plugin marketplace definition and the Distinct plugin sources maintained by Mangabey AB.

For installation, configuration, and usage, see **[distinct.sh](https://distinct.sh)**.
